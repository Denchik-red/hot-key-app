generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model deaths {
  id        String  @id
  userName  String  @unique(map: "sqlite_autoindex_deaths_2") @map("user_name")
  count     Int     @default(0) 
  imageUrl String? @map("image_url")
}
